import {createMoistMainChart} from "../createMoistMainChart";

export const updateMoistChart = (data: any, root: any, isMobile: any, fullDatesArray: any, additionalChartData: any): void => {
  createMoistMainChart(data, root, isMobile, fullDatesArray, additionalChartData);
};